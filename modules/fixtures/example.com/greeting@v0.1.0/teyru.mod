// The module a test build depends on. Its path is example.com on purpose: no
// such repository exists, so nothing here can be fetched from the network by
// accident. The directory name spells the module path and the version, which
// is the layout the module cache and the local fetcher both read.
module example.com/greeting

teyru 1
