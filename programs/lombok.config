# Settings the Lombok compatibility pass reads. The search rule is Lombok's:
# from the source file's directory upwards, nearest setting wins. Only
# @CustomLog reads anything here; every other test program is unaffected.
lombok.log.custom.declaration = TestLog TestLog.of(NAME)
